# OpenOCD configuration for CH32V305RB via WCH-LinkE (SDI)
# Requires: brew install open-ocd  (>= 0.12)
# Note: if 'wch-link' interface or 'sdi' transport is not found, upstream OpenOCD may be too old.
# In that case use WCH's fork: https://github.com/openocd-org/openocd (wch branch)

adapter driver wlinke
adapter speed 1000
transport select sdi

wlink_set_address 0x08000000
set _CHIPNAME wch_riscv
sdi newtap $_CHIPNAME cpu -irlen 5 -expected-id 0x00001

set _TARGETNAME $_CHIPNAME.cpu

target create $_TARGETNAME.0 wch_riscv -chain-position $_TARGETNAME
$_TARGETNAME.0 configure  -work-area-phys 0x20000000 -work-area-size 10000 -work-area-backup 1
set _FLASHNAME $_CHIPNAME.flash

flash bank $_FLASHNAME wch_riscv 0x08000000 0 0 0 $_TARGETNAME.0

echo "Ready for Remote Connections"
